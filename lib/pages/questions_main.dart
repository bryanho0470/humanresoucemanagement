import 'package:flutter/material.dart';

class Question_List extends StatelessWidget {
  const Question_List({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
